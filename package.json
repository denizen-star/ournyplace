{
  "name": "nyhome",
  "version": "1.0.0",
  "private": true,
  "description": "Private local PWA for choosing a NYC apartment together",
  "scripts": {
    "dev": "npx netlify dev",
    "migrate": "node scripts/migrate.js"
  },
  "dependencies": {
    "@planetscale/database": "^1.20.1",
    "dotenv": "^17.3.1"
  },
  "devDependencies": {
    "netlify-cli": "^24.0.1"
  }
}
